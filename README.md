# ds-algorithms

Minimal C data structures and algorithms demos.

## Build

- `make` or `make build`
- `make run`
- `make clean`

## Manual build

```sh
clang -I include src/main.c src/lists/*.c src/trees/*.c -o dsalgo
```
